# 此文件仅作说明，workflow 不会复制它。
#
# OpenWrt v25.12.4 源码自带的 feeds.conf.default 已锁定各 feed 的 commit，
# 例如 git.openwrt.org/feed/packages.git^f91b06b3...
# 请勿用分支名 openwrt-25.12 覆盖，否则 feeds update 会失败。
#
# 自定义 feed（luci-theme-edge2）由 scripts/diy-part1.sh 追加到官方 feeds.conf.default。
